{
  "name": "amazon-clone",
  "version": "1.0.0",
  "description": "Full-stack Amazon clone application",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "install-all": "cd frontend && npm install"
  }
}
